from setuptools import find_packages, setup

setup(
    name="modbus_tk",  # 包的名称
    version="1.0.0",  # 版本号
    packages=find_packages(),  # 自动找到所有的包，包含在安装中
    scripts=[],  # 可选：包含要安装的脚本文件列表
    author="Your Name",  # 作者名
)
